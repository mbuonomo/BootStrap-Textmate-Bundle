Twitter Bootstrap - TextMate BUNDLE

Simple bundle to deal with the Twitter BootStrap

How-To create the sample page

boot_layout + tab
boot_hero + tab
boot_container + tab
boot_span + tab + 4
boot_span + tab + 4
boot_span + tab + 4

I will add some other snippets in the future

